--- a/app/telegram_admin_manager.py
+++ b/app/telegram_admin_manager.py
@@ -67,7 +67,7 @@
 
 
 APP_NAME = "Telegram Admin Manager"
-APP_VERSION = "0009"
+APP_VERSION = "0010"
 DEFAULT_UPDATE_MANIFEST = "https://raw.githubusercontent.com/youngcitizencosmopolitan/telegram-admin-manager-updates/main/update.json"
 
 AUTO_CONTROLLER_LABEL = "Авто — владелец / админ с правом добавлять админов"
@@ -5550,6 +5550,12 @@
             "или администратора с правом добавлять администраторов. "
             f"Цель смогли открыть {checked} аккаунт(а/ов)."
         )
+
+    def maximum_admin_policy_key(self, title: str = "") -> str:
+        """Stable cache key for the uploader's maximum-rights admin policy."""
+        preset = "full_add_admins"
+        anonymous = bool(self.store.data.get("admin_anonymous", True))
+        return build_admin_policy_key(preset, title, anonymous, self._requested_admin_flags(preset))
 
     def _requested_admin_flags(self, preset: str) -> dict[str, bool]:
         """Desired policy for our own admins: maximum rights, always.
